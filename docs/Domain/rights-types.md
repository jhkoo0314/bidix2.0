

```md
# 🛡️ Rights Type Specification for v2.0
**Version:** 2.0
**Last Updated:** 2025-11-09

> 본 문서는 경매에서 마주치는 주요 권리 유형을 **표준화(SSOT)**하고, `RightsEngine`의 분석 규칙을 정의합니다.
> **코드 기준:** `/lib/types/rights.ts` (`RightType` enum)
> **정책 기준:** `/lib/policy/defaultPolicy.ts`, `/lib/policy/rightsPolicy.ts`
> **엔진 기준:** `/lib/engines/rightsEngine.ts`

---

## 1. 권리 분석 개요

- **목표:** `CourtDocsNormalized` 데이터를 기반으로 각 권리의 소멸/인수 여부를 판단하고, 최종적으로 낙찰자가 부담해야 할 금전적/비금전적 리스크를 정량화한다.
- **입력:** `CourtDocsNormalized` (특히 `registeredRights`, `occupants`, `baseRightDate`)
- **정책:** `defaultPolicy` 및 `difficultyPolicies`에서 파생된 최종 `Policy` 객체
- **출력:** `Rights` 객체
    - `assumableRightsTotal` (총 인수 금액)
    - `evictionCostEstimated` (예상 명도 비용)
    - `evictionRisk` (명도 위험도)

---

## 2. 권리 유형 표준 정의

| Enum 값 | 표시명 | 기본 인수여부* | 위험도(0~5) | 요지 |
|:---|:---|:---|:---:|:---|
| `mortgage` | 근저당권 | **말소** | 1 | 대표적인 말소기준권리. 담보권. |
| `provisional_seizure` | 가압류 | **말소** | 2 | 채권 보전을 위한 임시 조치. |
| `seizure` | 압류 | **말소** | 2 | 국가 기관의 강제 집행 (예: 세금 체납). |
| `leasehold` | 전세권 | 조건부 | 3-4 | 말소기준권리보다 선순위이고, 배당요구를 안 하면 인수. |
| `tenant_protected` | 임차인(대항력O)| **인수** | 4-5 | 말소기준권리보다 전입이 빠른 임차인. 보증금 인수. |
| `tenant_unprotected`| 임차인(대항력X)| **말소** | 1 | 말소기준권리보다 전입이 늦은 임차인. |
| `lien` | 유치권 | 조건부 | 5 | 공사대금 등 채권을 변제받기 위해 부동산을 점유할 권리. |
| `statutory_surface`| 법정지상권 | **인수** | 5 | 토지와 건물의 소유자가 달라졌을 때 건물 소유자를 위한 권리. |
| `grave_right` | 분묘기지권 | **인수** | 4 | 타인의 토지에 분묘를 소유하기 위한 권리. |
| `tax_lien` | 조세채권 | 조건부 | 3-4 | 세금 체납. '당해세'는 배당 순위가 매우 높음. |
| `provisional_registry`| 가등기 | 조건부 | 4 | 소유권 이전 등 본등기를 할 수 있는 권리를 보전. |
| ... | (기타 7종) | ... | ... | (생략) |

\* **"기본 인수여부"는 `CourtDocsNormalized.baseRightDate`와 각 권리의 `date` 또는 `moveInDate` 선후 관계, 그리고 `dividendRequested` 여부에 따라 `RightsEngine`이 동적으로 최종 판단합니다.**

---

## 3. 인수/말소 판단 규칙 (엔진 요약)

1.  **말소기준권리(Base Right) 탐색:** `CourtDocsNormalized.registeredRights` 배열에서 `isBaseRight: true`인 권리를 찾고, 그 날짜(`baseRightDate`)를 확정한다.
2.  **후순위 권리 처리:** `baseRightDate`보다 늦게 설정된 모든 등기 권리(`registeredRights`)는 원칙적으로 **'소멸'** 처리한다.
3.  **선순위 임차인 처리:** `baseRightDate`보다 `moveInDate`가 빠른 임차인(`occupants`)은 **'대항력 있음'**으로 판단한다.
4.  **배당 요구 확인:** '대항력 있는 임차인'이 배당요구(`dividendRequested: true`)를 했는지 여부에 따라, 배당 후 잔액만 인수할지, 보증금 전액을 인수할지 분기 처리한다.
5.  **기타 인수 권리:** 법정지상권, 분묘기지권 등은 원칙적으로 **'인수'** 처리한다.

---

## 4. 위험도 스케일 (0~5)

| 점수 | 레이블 | 의미(요약) | 예시 |
|:---:|:---|:---|:---|
| 0 | None | 위험 없음 | 단순 후순위 권리, 소유자 점유 |
| 1 | Very Low | 실무 영향 미미 | 대항력 없는 임차인 (명도 저항 낮음) |
| 2 | Low | 주의 필요 | 다수의 후순위 압류/가압류 |
| 3 | Medium | 권리 검토 필수 | 선순위 임차인 (보증금이 낮거나, 전액 배당 예상) |
| 4 | High | 인수 가능성 큼 | 선순위 임차인 (보증금 일부/전액 인수 예상) |
| 5 | Critical | 고난이도/고위험 | 법정지상권, 유치권, 선순위 가처분 등 |

> `rightsPolicy.ts`에서 유형별 기본 위험도를 정의하고, `RightsEngine`은 `difficulty` 및 시나리오 특성에 따라 가중치를 적용하여 최종 위험도를 산출합니다.

---

## 5. `RightsEngine` 파이프라인 (v2.0 기준)

```
CourtDocsNormalized (from Generator)
├─ registeredRights[] (등기된 모든 권리 목록)
├─ occupants[]      (모든 점유자/임차인 목록)
└─ baseRightDate    (말소기준권리의 날짜)
│
▼
Policy (from Action)
├─ rights.evictionCostBase
└─ rights.evictionRiskWeight
│
▼
RightsEngine (within AuctionEngine)
│
└─ Rights (최종 권리 분석 결과)
   ├─ assumableRightsTotal
   ├─ evictionCostEstimated
   ├─ evictionRisk
   └─ breakdown[]```

---

## 6. 정책/계산 영향 표 (최신 v2.0 기준)

| 항목 | 정책 키 (`policy.rights.*`) | 설명 |
|:---|:---|:---|
| **기본 명도 비용** | `evictionCostBase` | 협상/이사 등에 필요한 기본적인 명도 비용의 기준값. |
| **위험 가중치** | `evictionRiskWeight` | 임차인의 대항력 등 위험 요소를 금전적 비용(명도 비용)으로 환산할 때의 가중치. |

---

## 7. 관련 코드/문서 링크

- **타입:** `lib/types/rights.ts`, `lib/types/courtDocs.ts`
- **엔진:** `lib/engines/rightsEngine.ts`
- **정책:** `lib/policy/defaultPolicy.ts`, `lib/policy/difficultyPolicies.ts`
- **설계:** `docs/Domain/court-docs.md`

---
**END OF FILE**
```

